# Runtime dependencies. Used by Vercel (plain `pip install -r`) and any non-package install.
# Local development / the `ghostline` CLI entrypoint use pyproject.toml instead.
calle-ai>=0.7.0
fastapi>=0.115
uvicorn[standard]>=0.32
jinja2>=3.1
python-multipart>=0.0.12
pydantic>=2.9
pydantic-settings>=2.6
python-dotenv>=1.0
pyyaml>=6.0
phonenumbers>=8.13
httpx>=0.27
anthropic>=0.40
segno>=1.6
