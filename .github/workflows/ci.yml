name: ci

on:
  push:
    branches: [main]
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - run: pip install -e ".[dev]"
      - name: lint
        run: ruff check ghostline tests scripts skills
      - name: tests
        run: pytest -q
      - name: skill scripts run (stdlib only)
        run: |
          cd skills/phone-claim-verifier
          python scripts/plan.py examples/providers.csv --pack examples/healthcare.json > /dev/null
          echo "skill plan.py OK"
