# GHOSTLINE — Decision Log

One row per non-trivial technical/strategic decision. Newest at top. Link back to the master
doc section it implements or amends.

| # | Date | Decision | Rationale | Status |
|---|---|---|---|---|
| D-014 | 2026-08-31 | **Fixture phone numbers use the `+1 202 555 01xx` block**, not `+1 555 …`. | libphonenumber rejects 555 as an area code as invalid; 202-555-0100..0199 is the genuine NANP fictional-use range and validates. All test/fixture numbers updated. | Locked |
| D-013 | 2026-08-31 | **Repo layout: a `ghostline/` package** (models, claim_pack, policy_gate, call_engine, calle_normalize, extractor, verdict, corrections, pipeline, replay, csv_io, cli) + `examples/`, `replay/fixtures/`, `tests/`. The `console/` (FastAPI) and `skills/phone-claim-verifier/` land alongside. | Master doc §5.3 sketched a flat `core/` with `.py/.ts`; a real Python package with a namespace is cleaner and `pip install -e` / entrypoint work properly. Master doc §5.3 intent (one engine, thin layers) preserved. | Locked |
| D-012 | 2026-08-31 | **Two CALL-E surfaces, on purpose:** Python SDK (`calls.create`) powers the hosted console + CLI engine; **MCP** (`plan_call`/`run_call`/`get_call_run`) powers the `skills/phone-claim-verifier/` Skill contribution. | Due diligence found the master doc's `plan_call/run_call/get_call_run` are the real **MCP** tool names. Skills run in agent environments where MCP is native; SDK is native for a server app. Using both = maximum genuine CALL-E surface for Technical Implementation, and maps to the hackathon's `apps/` + `skills/` structure. See [CALL_E_INTEGRATION.md](../research/CALL_E_INTEGRATION.md) §4a. | Locked |
| D-011 | 2026-08-31 | **"Cancellation" = client-side abandon only.** Ghostline stops polling and marks the verification abandoned; the CALL-E call still completes and bills. Documented as a known limitation. | No cancel endpoint exists in CALL-E API 0.6.0 (see [CALL_E_INTEGRATION.md](../research/CALL_E_INTEGRATION.md) §7). Amends master doc §5.6/§5.10. | Locked |
| D-010 | 2026-08-31 | **Cherry (e) "audio playback" → "confidence-colored transcript replay"**: re-render `transcript_turns` with `offset_seconds` timing, highlight the verbatim evidence span, color by confidence. No audio. | CALL-E API exposes no recording/audio URL. Same review value. Amends master doc §4.12e. | Locked |
| D-009 | 2026-08-31 | **Ghostline runs its own extraction pass** over `transcript_turns` (verbatim-span-required); CALL-E's own `structured_result` / `evidence` / `completion_confidence` are stored as a *secondary* signal only. | CALL-E's `evidence` is paraphrased summary strings, not verbatim; our thesis needs a verbatim span. Bonus demo beat: Ghostline abstains where CALL-E was "high" confidence. See [CALL_E_INTEGRATION.md](../research/CALL_E_INTEGRATION.md) §4. | Locked |
| D-008 | 2026-08-31 | **Build on `client.calls.create` (+ `result_schema`, explicit `recipients`, `Idempotency-Key`)** as the primary runtime path. Optionally publish **one** dashboard Goal and show `client.goals.run` in the demo as a bonus. | `calls.create` keeps the claim pack as in-repo, reproducible config; Goals are dashboard-authored (not version-controllable). Master doc's `plan_call/run_call/get_call_run` do not exist. See [CALL_E_INTEGRATION.md](../research/CALL_E_INTEGRATION.md) §2. | Locked |
| D-007 | 2026-08-31 | **Primary CALL-E integration = Python SDK** (`calle-ai` v0.7.0, import `calle`); PR contribution = Agent Skill; CLI is dev-only. | Binding Project Requirements clause omits CLI; SDK gives the cleanest "imported and called at runtime" evidence for Stage One + Technical Implementation. See [HACKATHON_ANALYSIS.md](../research/HACKATHON_ANALYSIS.md) §2. | Locked |
| D-006 | 2026-08-31 | **Scope = full master-doc scope; nothing minimised.** All 13 cherries, both packs demoed, derived calls, 40–50-call benchmark. Master doc §8.1 cut-order is a slip-triggered contingency only. | Entrant's explicit direction. | Locked |
| D-005 | 2026-08-31 | **No AI-tool attribution anywhere** (source, commits, PR, Devpost, video, metadata). No `Co-Authored-By` AI trailer. | Entrant's direction; hackathon sole-authorship framing. See [`RULES.md`](../RULES.md) §1. | Locked |
| D-004 | 2026-08-31 | **No `git push` / remote-mutating command without explicit per-action approval**, even after a remote is connected. | Entrant's direction. [`RULES.md`](../RULES.md) §2. | Locked |
| D-003 | 2026-08-31 | **No paid services or API spend without approval.** CALL-E free 20 + free `+200` only. Free-tier hosting only. | Entrant's direction. [`RULES.md`](../RULES.md) §3. | Locked |
| D-002 | 2026-08-31 | **Build the CLI vertical slice first (Days 2–5), then the console on top of it.** | De-risks the CALL-E integration before spending time widening it; the console becomes pure UI over a proven engine. Master doc §5.7 already says "build replay before the live call engine". | Locked |
| D-001 | 2026-08-31 | **Re-anchor the build plan to a 2026-08-31 start / Sep 12 submit target.** Master doc §8 day-by-day is superseded by [`ROADMAP.md`](ROADMAP.md); master doc idea/spec/kill-list/safety unchanged. | Master doc §8 assumed an Aug 18 start; ~13 days elapsed with no code. | Locked |

## Open decisions (need entrant input)

| # | Question | Options | Recommendation |
|---|---|---|---|
| O-003 | Hosting target for the console | Render / Railway / Fly.io free tier · Vercel (frontend) + separate backend host · a VPS the entrant owns | A single small always-on free-tier host (Render/Railway/Fly) serving FastAPI + server-rendered HTML — must stay up through Oct 13 |
| ~~O-002~~ → **D-007** | CALL-E integration path | — | **RESOLVED 2026-08-31:** primary runtime = **CALL-E Python SDK** (binding Project Requirements clause names "API or SDKs for Python/TypeScript, or Skill or MCP" — **CLI is not a binding path**, see [HACKATHON_ANALYSIS.md](../research/HACKATHON_ANALYSIS.md) §2). PR contribution = Agent Skill. CLI = dev convenience only. Master doc's `plan_call/run_call/get_call_run` names to be verified against `docs.heycall-e.com` Day 1 (CALL-E docs describe "goals"). |
| O-001 | Backend language | Python (FastAPI) — matches existing `.venv` / PyCharm project · Node/TS | **Python/FastAPI** — the repo is already a Python project; CALL-E has a Python SDK. |
